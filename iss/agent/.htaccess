RewriteEngine On

# Set the base for the rewrite rules to the current directory (agent/)
RewriteBase /iss/agent/

# Redirect only if the request is for the root of the 'agent' directory (empty string after RewriteBase)
RewriteRule ^$ dashboard/ [L,R=301]